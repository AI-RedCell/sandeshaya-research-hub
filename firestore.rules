rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Users: Allow read for all authenticated users (Dashboard Admin)
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Responses: Allow read/delete for all authenticated users (Dashboard Admin)
    match /responses/{userId} {
      allow read, delete: if request.auth != null;
      // Allow write (create/update) only if auth matches docId (the userId)
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    match /metadata/{docId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
